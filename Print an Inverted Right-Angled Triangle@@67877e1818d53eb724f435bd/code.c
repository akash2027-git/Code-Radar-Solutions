#include <stdio.h>

int main() {
    int a;
    scanf("%d", &a);
    for(int i = 1; i < n+1; i++) {
        for(int j < i+1; j > 1; j--) {
            printf("*");
        }
        printf();
    }
}